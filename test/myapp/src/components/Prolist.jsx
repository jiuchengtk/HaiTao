import React from 'react';
import { Link } from 'react-router-dom';
import './pro.scss';
export default (props) => {
  // console.log(props)
  const { prolist } = props;
  return (
    <ul className="prolist">
      
    </ul>
  )
}
